package javaapplication.pkg3.pkg7;
   

// Actividad resuelta 3.7

// Escribir todos los multiplos de 7 menores que 100
// vamos a utilzar un bucle "for" inicializando la "i" a "0"
// intentando hasta que el valor * supere 100
// los multiplos de 7, se caracterizan por que se diferrencian en 7.*/
/**
 *
 * @author cafet
 */
public class JavaApplication37 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
      for (int i = 0; i < 100; i += 7) {
          System.out.println("i");
          
    // cuando el bloque de instrucciones de "for","while" o du-while esta formado
    // por una sola instruccion, no precisa de llave { }
    // aunque, por claridad en el codigo,se aconseja ponerlas
    
      }
        
        
        
    }
    
}
